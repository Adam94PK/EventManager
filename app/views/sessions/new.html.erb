<h1>Log in</h1>
<div>
	<%= form_for :session, url: login_path do |f| %>

		<p>
		<%= f.label :user_name %>
		<%= f.text_field :user_name %>
		</p>

		<p>
		<%= f.label :password %>
		<%= f.password_field :pasword %>
		</p>

		<p>
		<%= f.submit 'Log in' %>
		</p>

	<% end %>
</div>